0. This plugin was written to help you understand how to use SQLiteLib.
1. SQLiteLibTester is an Eclipse project file, but you can probably import in Intellij too.
2. If you want to build this plugin from the source, you need to add Spigot API and SQLiteLib into project build path (because this project does not use Maven).
3. Put cat.jpg at the root of your server folder (where your server bukkit jar file is located).